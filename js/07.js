// String functions

// trim removes the spaces
let str="   hel   lo";
console.log(str);
str=str.trim();
console.log(str);
// to tupper case, lower case
console.log(str.toUpperCase());
// slice method 
console.log(str.slice(0,7));